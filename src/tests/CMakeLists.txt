# This CMakeLists.txt is for subdirectory tests
# Tests are now configured in the root CMakeLists.txt
# This file is kept for compatibility but tests should be configured at root level

message(STATUS "[TESTS] Test configuration is handled by root CMakeLists.txt")
